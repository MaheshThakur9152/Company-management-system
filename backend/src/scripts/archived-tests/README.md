# Archived debug/test scripts

This folder contains ad-hoc scripts that were used for debugging, inspecting invoices and one-off repairs. They are archived to keep the main `src/scripts` folder clean. These scripts are preserved for historical reference only and should not be used as part of normal workflows.

If you want to re-enable any script, move it back to `src/scripts/` and review it before running.
